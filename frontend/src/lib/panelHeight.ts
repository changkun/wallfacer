// Storage key for the dock panel height, persisted across reloads. The
// clamping lives in the dock store (regionMax), which owns the layout.

export const PANEL_HEIGHT_KEY = 'wallfacer-panel-height';
