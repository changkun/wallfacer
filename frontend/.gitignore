node_modules/
dist/
*.tsbuildinfo
src/**/*.js
src/**/*.vue.js
# Vendored source .js (not build output) must stay tracked.
!src/vendor/**/*.js

# Local migration verification tooling (not product)
.parity/
package-lock.json
