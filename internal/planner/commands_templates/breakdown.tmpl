Break down the spec at {{.FocusedSpec}} into implementable sub-specs or tasks. Follow the spec document model: each child must have valid YAML frontmatter with title, status, depends_on, affects, effort, created, updated, author, and dispatched_task_id fields. Order tasks so dependencies flow forward.