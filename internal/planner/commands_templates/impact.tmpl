Analyze what existing code and specs would be affected by implementing {{.FocusedSpec}}. Check the affects list in the frontmatter, scan for reverse dependencies (other specs that depend on this one), and identify any shared interfaces or types that would change.