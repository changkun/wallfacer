Finalize the completed spec at {{.FocusedSpec}}. Verify all leaf specs in the subtree have status complete. Run tests to confirm they pass. Then update the spec: set status to complete, set updated to today, add an Outcome section (summary paragraph, What Shipped subsection, Design Evolution subsection documenting deviations from original spec). Update specs/README.md status entries. Check reverse dependencies — flag specs that were blocked by this one and are now unblocked. Do NOT push.