Refine the spec at {{.FocusedSpec}} so it accurately reflects the current state of the codebase. For each item in the spec, search the codebase to determine if it is done, partially done, not started, or obsolete. Remove done items (summarize briefly in a "Current State" section if they provide context). Update partially done items to reflect the current starting point. Keep not-started items but fix stale file paths and function names. Remove obsolete items. Update frontmatter: set updated to today, adjust status/affects/effort if warranted. {{if .Args}}Additional guidance: {{.Args}}.{{end}}