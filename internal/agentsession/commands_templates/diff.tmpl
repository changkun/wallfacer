Compare the implementation of {{.FocusedSpec}} against its acceptance criteria after the dispatched task completed. {{if .Args}}Commit range: {{.Args}}.{{else}}Infer the commit range from the dispatched task's worktree diff or git history.{{end}} For each spec item, classify as: satisfied, diverged (spec said X, impl does Y), not implemented, or superseded. Identify unspecified work (changes not traceable to any spec item). Compute drift level: minimal (>90% satisfied), moderate (70-90%), or significant (<70%). Append an Outcome section to the spec with the drift assessment, divergences, and unspecified work. If drift is significant, set the spec status to stale instead of complete.