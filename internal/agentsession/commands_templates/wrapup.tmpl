Finalize the completed spec at {{.FocusedSpec}}. Verify all leaf specs in the subtree have status complete. Run tests to confirm they pass. Then update the spec: set updated to today and add an Outcome section (summary paragraph, What Shipped subsection, Design Evolution subsection documenting deviations from original spec). Reaching complete is a server transition, not a frontmatter edit: a dispatched spec gets there through the drift pipeline on task-done, and a directly implemented one needs POST /api/specs/transition with action force-complete. Update specs/README.md status entries. Check reverse dependencies — flag specs that were blocked by this one and are now unblocked. Do NOT push.