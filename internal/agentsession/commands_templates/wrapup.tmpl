Finalize the completed spec at {{.FocusedSpec}}. Verify all leaf specs in the subtree have status complete. Run tests to confirm they pass. Then update the spec: set updated to today and add an Outcome section (summary paragraph, What Shipped subsection, Design Evolution subsection documenting deviations from original spec). Reaching complete is never a direct frontmatter jump from validated: a dispatched spec gets there through the server drift pipeline on task-done, while a directly implemented one walks validated to testing as a legal frontmatter edit (no API action enters testing) and then calls POST /api/specs/transition with action force-complete, which rejects any spec not already in testing. Update specs/README.md status entries. Check reverse dependencies — flag specs that were blocked by this one and are now unblocked. Do NOT push.