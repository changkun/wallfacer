Break down the spec at {{.FocusedSpec}} into child specs. Determine the mode automatically from the spec's lifecycle state: design mode (sub-design specs with Options and Open Questions, status drafted) if the spec is vague/drafted with unresolved questions, or tasks mode (implementation leaf specs with Goal, What to do, Tests, Boundaries, status validated) if the spec is validated with a clear implementation plan. {{if .Args}}Use mode: {{.Args}}.{{end}} Follow the spec document model: each child must have valid YAML frontmatter with title, status, depends_on, affects, effort, created, updated, author, and dispatched_task_id fields. Order children so dependencies flow forward. Index the breakdown on the parent spec with a summary table and Mermaid dependency graph.