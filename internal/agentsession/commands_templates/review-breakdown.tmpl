Review the task breakdown of {{.FocusedSpec}} for correctness before implementation. Check: (1) dependency correctness — all depends_on paths resolve, no cycles, direction matches affects overlap; (2) task sizing — effort vs actual scope of affects list; (3) spec coverage — every spec item has a corresponding child task, no untraceable tasks; (4) boundary conflicts — overlapping affects between independent siblings; (5) test completeness — every child has a non-empty Tests section; (6) critical path — longest sequential chain, parallelism opportunities. Report a structured verdict: PASS or NEEDS REVISION with specific issues. Do NOT modify any files.