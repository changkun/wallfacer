Review the implementation of {{.FocusedSpec}} against its acceptance criteria. {{if .Args}}Commit range: {{.Args}}.{{else}}Infer the commit range from git history.{{end}} Check: (1) each "What to do" step or acceptance criterion — classify as implemented, partially implemented, or not implemented; (2) unintended changes — files modified outside the affects list; (3) test coverage — each Tests requirement has a corresponding test; (4) boundary compliance — no changes to forbidden areas; (5) documentation — required doc updates were made. Report a structured verdict: COMPLETE, INCOMPLETE, or NEEDS FIXES. Do NOT modify any files.