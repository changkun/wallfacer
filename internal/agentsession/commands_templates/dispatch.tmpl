Prepare {{.FocusedSpec}} for dispatch to the task board. Verify the spec status is "validated", all depends_on specs are "complete", and the spec is a leaf (no children). Set dispatched_task_id to a placeholder UUID and update specs/README.md accordingly.